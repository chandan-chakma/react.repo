import React from 'react';

const Dial = (props) => {
    return (
        <div>
            <h1>name:{props.name}</h1>
            <p>steps: {props.steps}</p>

        </div>
    );
};

export default Dial;